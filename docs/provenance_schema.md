# Provenance Schema

Each provenance record includes project id, phase, agent, input, agent prompt, model version, sources used, retrieved passages, output summary, full output snapshot, human edits, rejected suggestions, validation notes, decision log, human decision, human comment, and timestamp. The browser implementation stores records in local storage and exports a complete audit package as JSON.
