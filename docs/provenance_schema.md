# Provenance Schema

Each provenance record includes project id, phase, agent, input, model version, sources used, output summary, human decision, human comment, and timestamp. The browser MVP stores records in local storage and exports them as JSON.
